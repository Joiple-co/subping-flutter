pre-commit:
  commands:
    formatting:
      glob: '*.dart'
      run: flutter format {staged_files} && git add {staged_files}
    linter:
      run: flutter analyze {staged_files}
    tests:
      run: flutter test 